import { PasswordMatchValidator } from './password-match-validator.validator';

describe('PasswordMatchValidator', () => {
  it('should create an instance', () => {
    expect(new PasswordMatchValidator()).toBeTruthy();
  });
});
