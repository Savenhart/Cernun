import { RegisterValidator } from './register-validator.validator';

describe('RegisterValidato', () => {
  it('should create an instance', () => {
    expect(new RegisterValidator()).toBeTruthy();
  });
});
